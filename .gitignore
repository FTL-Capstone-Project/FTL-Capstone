# dependencies
node_modules/
**/node_modules/

# env / secrets (never commit real keys — see .env.example)
.env
.env.local
**/.env
**/.env.local

# build output
dist/
build/
client/dist/

# prisma
server/src/prisma/*.db

# misc
.DS_Store
*.log
npm-debug.log*
*.bak
coverage/

# editor / IDE
.vscode/
.idea/
*.swp

# Claude Code project instructions (each team member keeps their own locally)
CLAUDE.md
personalNotes.md
file.txt
myNotes.txt

# Local scratch — generated test-run output (regenerable by re-running the harness)
scripts/out/
# Slide decks are heavy binaries; keep NEW ones out of git.
# (planning/Orbis_Demo.pptx + Orbis_Pod_Sync.pptx were committed before this rule and are kept
#  on purpose as demo deliverables — this rule only stops future decks from being added.)
*.pptx
