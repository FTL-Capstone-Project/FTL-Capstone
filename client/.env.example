# Client env — copy to client/.env.local (gitignored).
# Vite only exposes vars prefixed with VITE_.
VITE_CLERK_PUBLISHABLE_KEY="pk_test_..."   # from Clerk dashboard
VITE_API_URL="http://localhost:3001"       # where the server runs
